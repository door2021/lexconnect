services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: lexconnect
      POSTGRES_PASSWORD: lexconnect
      POSTGRES_DB: lexconnect
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: