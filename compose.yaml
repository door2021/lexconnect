services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: lawnet
      POSTGRES_PASSWORD: lawnet
      POSTGRES_DB: lawnet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: